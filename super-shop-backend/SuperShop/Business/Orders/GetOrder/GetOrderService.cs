using Microsoft.EntityFrameworkCore;
//using SuperShop.Data.Context;
//using SuperShop.Data.Entities;
namespace SuperShop.Business.Orders.GetOrder
{
    public class GetOrderService
    {
    //    private readonly CoreContext _coreContext;

    //    public GetOrderService(CoreContext coreContext)
    //    {
    //        _coreContext = coreContext;
    //    }

    //    public async Task<List<Order>> GetOrderByUser(long id)
    //    {
    //        return await _coreContext.Order.Where(o => o.UserId == id).OrderByDescending(o => o.OrderId).Take(10).ToListAsync();
    //    }

    //    public async Task<List<Order>> GetAll()
    //    {
    //        return await _coreContext.Order.OrderByDescending(o => o.OrderId).Take(10).ToListAsync();
    //    }
    }
}
